<!DOCTYPE html>
<html lang="en">
  <head>
    <title>ApexProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://getbootstrap.com/docs/5.2/assets/css/docs.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"></script>
  </head>

<body style="background-color:#E3DFD6;">
  <nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Apex Legends</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="<%= homepage_path %>">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= legends_path %>">Legends</a>
          </li><li class="nav-item">
            <a class="nav-link" href="<%= aboutproject_path %>">About</a>
          </li>
          </li><li class="nav-item">
            <a class="nav-link" href="/roles">Roles</a>
          </li>
        </ul>
        <form action="<%= search_path %>" method="get">
          <select name="roles">
          <% Role.all.each do |role| %>
            <option value="<%= role.id %>"><%= role.name %></option>
          <% end %>
          </select>
          <%= text_field_tag(:search, params[:search], :class => "form-control me-2") %>
            <%= submit_tag "Search", :class => "btn btn-outline-success", name: nil %>
        </form>
    </div>
  </div>
</nav>
  <% if @role_header %>
    <h1 class="text-center">Roles</h1>
  <% elsif @about_header %>
    <h1 class="text-center">About</h1>
  <% elsif @index_header || @role_all_legends %>
    <h1 class="text-center">Legends</h1>
  <% elsif @show_header %>
    <h1 class="text-center">Legend</h1>
  <% end %>
    <%= yield %>
</body>
</html>
